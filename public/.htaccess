# Passenger is configured in the Apache vhost conf (passenger.conf) by cPanel Application Manager.
# Do not add PassengerEnabled or other Passenger directives here — they are not permitted in .htaccess.
